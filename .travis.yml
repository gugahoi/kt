addons:
  apt:
    packages:
      - docker-ce

services:
  - docker

script:
  - docker-compose run --rm test
  - docker-compose run --rm kt join -e qa

after_success:
  - bin/deploy "${TRAVIS_TAG:-dev}"
